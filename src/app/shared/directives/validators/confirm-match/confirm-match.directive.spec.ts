import { ConfirmMatchDirective } from './confirm-match.directive';

describe('ConfirmMatchDirective', () => {
	it('should create an instance', () => {
		const directive = new ConfirmMatchDirective();
		expect(directive).toBeTruthy();
	});
});
